import "./testmods/mathutils";

fn main() {
    let result = mathutils.add(3, 4);
    println(str(result));

    let product = mathutils.multiply(6, 7);
    println(str(product));

    let biggest = mathutils.max_of(10, 20);
    println(str(biggest));
}
