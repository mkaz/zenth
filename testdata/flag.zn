// Test Flag() built-in with default values

fn main() {
    let debug = Flag(default=false);
    let times = Flag(default=5);
    let msg = Flag(default="Hello");

    Println("debug=" + Str(debug));
    Println("times=" + Str(times));
    Println("msg=" + msg);
}
