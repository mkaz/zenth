fn greet(name: Str, greeting: Str = "Hello") {
    Println(greeting + ", " + name + "!");
}

fn add(a: Int, b: Int, c: Int = 0) -> Int {
    return a + b + c;
}

fn main() {
    greet("Alice");
    greet("Bob", "Hi");
    Println(add(1, 2));
    Println(add(1, 2, 3));
}
