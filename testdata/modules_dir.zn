import "./testmods/shapes";

fn main() {
    let r = shapes.Rectangle(width=5.0, height=3.0);
    Println(Str(r.area()));
    Println(Str(r.perimeter()));

    let area = shapes.circle_area(4.0);
    Println(Str(area));
}
