// Test INT_MAX and INT_MIN built-in constants

fn main() {
    println(INT_MAX);
    println(INT_MIN);

    // Can use in expressions
    let big = INT_MAX - 1;
    println(big);

    // Comparison
    if INT_MAX > 0 {
        println("INT_MAX is positive");
    }
    if INT_MIN < 0 {
        println("INT_MIN is negative");
    }
}
