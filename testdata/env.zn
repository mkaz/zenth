fn main() {
    let home = Env("HOME");
    Println("HOME=" + home);

    let missing = Env("ZENTH_NONEXISTENT_VAR_12345", "fallback");
    Println("missing=" + missing);

    let empty_default = Env("ZENTH_NONEXISTENT_VAR_12345");
    Println("empty=" + empty_default);
}
