obj Point {
    x: int;
    y: int;
}

fn main() {
    var grid: array(Point) = [];
    let pt = Point(x=1, y=2);
    grid.add(pt);

    Println("len=" + Str(Len(grid)));
    Println("x=" + Str(grid[0].x) + " y=" + Str(grid[0].y));

    grid = [];
    Println("cleared=" + Str(Len(grid)));
}
