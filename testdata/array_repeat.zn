// Test array repeat method

fn main() {
    // Basic repeat - fill with zeros
    let zeros = [0].repeat(5);
    Println(Len(zeros));
    for z in zeros {
        Print(Str(z) + " ");
    }
    Println("");

    // Repeat strings
    let dashes = ["-"].repeat(3);
    Println(dashes.join(""));

    // Repeat pattern
    let pattern = [1, 2].repeat(6);
    for p in pattern {
        Print(Str(p) + " ");
    }
    Println("");

    // Repeat with single element
    let trues = [true].repeat(3);
    Println(Len(trues));
}

// Expected output:
// 5
// 0 0 0 0 0
// ---
// 1 2 1 2 1 2
// 3
