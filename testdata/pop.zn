// Test .pop() method on arrays
fn main() {
    var c = [0, 1, 2, 3, 4, 5, 6];

    let last = c.pop();
    Println("last=" + Str(last));

    let four = c.pop(4);
    Println("four=" + Str(four));

    Println("len=" + Str(Len(c)));
}
