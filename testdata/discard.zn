// Test _ discard in for-in loops

fn main() {
    // _ as value in for-in (discard each element)
    var count = 0;
    for _ in Range(0, 5) {
        count++;
    }
    Println("count=" + Str(count));

    // _ as index in for-in
    let items = ["a", "b", "c"];
    for _, item in items {
        Print(item);
    }
    Println("");

    // _ as value with index
    for i, _ in items {
        Print(Str(i));
    }
    Println("");
}
